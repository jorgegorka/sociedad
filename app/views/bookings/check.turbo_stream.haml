= turbo_stream.update 'resources-list' do
  - if @available_resources.first.empty?
    = render partial: 'bookings/error', collection: @available_resources.last
  - else
    = render partial: 'bookings/resource_booking', collection: @available_resources.first
